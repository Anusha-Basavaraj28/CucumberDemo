package com.demo.StepDefinitions;

import java.time.Duration;
import java.util.List;

import org.junit.After;
import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import com.demo.Utility.DataReader;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class loginStepDefinitions {

	/*
	 * WebDriver driver;
	 * 
	 * @Given("Browser is open") public void browser_is_open() {
	 * 
	 * driver=new ChromeDriver();
	 * 
	 * }
	 * 
	 * @And("User opens the application login page") public void
	 * user_opens_the_application_login_page() {
	 * 
	 * driver.get("https://example.testproject.io/web/");
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
	 * driver.manage().timeouts().pageLoadTimeout(Duration.ofSeconds(20)); }
	 * 
	 * 
	 * 
	 * @When("user enters valid email as {string} and password as {string}") public
	 * void user_enters_valid_email_as_and_password_as(String userName, String pwd)
	 * { driver.findElement(By.id("name")).sendKeys(userName);
	 * driver.findElement(By.id("password")).sendKeys(pwd); }
	 * 
	 * 
	 * @When("user enters valid email and password from excel sheet row {string}")
	 * public void user_enters_valid_email_and_password_from_excel_sheet_row(String
	 * row) throws Exception {
	 * 
	 * String[] s= DataReader.getDataFromExcel(row);
	 * 
	 * 
	 * 
	 * driver.findElement(By.id("name")).sendKeys(s[0]);
	 * driver.findElement(By.id("password")).sendKeys(s[1]);
	 * 
	 * }
	 * 
	 * 
	 * 
	 * @And("user clicks on login button") public void user_clicks_on_login_button()
	 * { driver.findElement(By.id("login")).click(); }
	 * 
	 * @Then("user should be able to logged in and navigated to home page") public
	 * void user_should_be_able_to_logged_in_and_navigated_to_home_page() { boolean
	 * status= driver.findElement(By.id("logout")).isDisplayed();
	 * Assert.assertTrue(status); }
	 * 
	 * @And("close the browser") public void close_the_browser() { driver.close(); }
	 * 
	 */
	  
	 
}
